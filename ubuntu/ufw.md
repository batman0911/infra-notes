# UFW

Enable firewall

```shell
sudo ufw enable
```

Check status

```shell
sudo ufw status numbered
```

Allow port

```shell
sudo ufw allow $PORT
```

Delete port

```shell
sudo ufw delete $NUMBER
```